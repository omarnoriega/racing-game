# Azure DevOps Pipeline - Racing Game
# CI/CD para Azure Container Apps

trigger: none
pr:
  branches:
    include:
      - main
      - develop

variables:
  # Azure Container Registry
  - name: acrName
    value: 'racinggamecr' 
  - name: acrLoginServer
    value: '$(acrName).azurecr.io'
  
  # Azure Container Apps
  - name: resourceGroup
    value: 'racing-game-rg' 
  - name: containerAppEnvironment
    value: 'racing-game-env' 
  - name: backendAppName
    value: 'racing-game-backend'
  - name: frontendAppName
    value: 'racing-game-frontend'
  
  # Docker Images
  - name: backendImageName
    value: 'racing-game-backend'
  - name: frontendImageName
    value: 'racing-game-frontend'
  - name: imageTag
    value: '$(Build.BuildId)'
  
  # Azure Subscription
  - name: azureSubscription
    value: 'Azure-Racing-Game' 

  # Configuración
  - group: 'racing-game-config' 

stages:
  - stage: Build
    displayName: 'Build and Push Docker Images'
    jobs:
      - job: BuildBackend
        displayName: 'Build Backend'
        pool:
          vmImage: 'ubuntu-latest'
        steps:
          - task: Docker@2
            displayName: 'Login to ACR'
            inputs:
              command: login
              containerRegistry: '$(acrName)'

          - task: Docker@2
            displayName: 'Build Backend Image'
            inputs:
              command: build
              repository: $(backendImageName)
              dockerfile: 'server/Dockerfile'
              containerRegistry: '$(acrName)'
              tags: |
                tap-$(imageTag)
                tap-latest

          - task: Docker@2
            displayName: 'Push Backend Image'
            inputs:
              command: push
              repository: $(backendImageName)
              containerRegistry: '$(acrName)'
              tags: |
                tap-$(imageTag)
                tap-latest

      - job: BuildFrontend
        displayName: 'Build Frontend'
        pool:
          vmImage: 'ubuntu-latest'
        steps:
          - task: Docker@2
            displayName: 'Login to ACR'
            inputs:
              command: login
              containerRegistry: '$(acrName)'

          - task: Docker@2
            displayName: 'Build Frontend Image'
            inputs:
              command: build
              repository: $(frontendImageName)
              dockerfile: 'client/Dockerfile'
              containerRegistry: '$(acrName)'
              arguments: |
                --build-arg REACT_APP_API_URL=$(REACT_APP_API_URL)
                --build-arg REACT_APP_WS_URL=$(REACT_APP_WS_URL)
              tags: |
                tap-$(imageTag)
                tap-latest

          - task: Docker@2
            displayName: 'Push Frontend Image'
            inputs:
              command: push
              repository: $(frontendImageName)
              containerRegistry: '$(acrName)'
              tags: |
                tap-$(imageTag)
                tap-latest

  - stage: DeployDev
    displayName: 'Deploy to Development'
    dependsOn: Build
    condition: and(succeeded(), eq(variables['Build.SourceBranch'], 'refs/heads/develop'))
    jobs:
      - deployment: DeployToDev
        displayName: 'Deploy to Dev Environment'
        environment: 'Development'
        pool:
          vmImage: 'ubuntu-latest'
        strategy:
          runOnce:
            deploy:
              steps:
                - task: AzureCLI@2
                  displayName: 'Deploy Backend to Container App'
                  inputs:
                    azureSubscription: '$(azureSubscription)'
                    scriptType: 'bash'
                    scriptLocation: 'inlineScript'
                    inlineScript: |
                      az containerapp update \
                        --name $(backendAppName) \
                        --resource-group $(resourceGroup) \
                        --image $(acrLoginServer)/$(backendImageName):tap-latest \
                        --set-env-vars \
                          NODE_ENV=development \
                          PORT=3001 \
                          JWT_SECRET=$(JWT_SECRET) \
                          CORS_ORIGIN=$(CORS_ORIGIN_DEV)

                - task: AzureCLI@2
                  displayName: 'Deploy Frontend to Container App'
                  inputs:
                    azureSubscription: '$(azureSubscription)'
                    scriptType: 'bash'
                    scriptLocation: 'inlineScript'
                    inlineScript: |
                      az containerapp update \
                        --name $(frontendAppName) \
                        --resource-group $(resourceGroup) \
                        --image $(acrLoginServer)/$(frontendImageName):tap-latest